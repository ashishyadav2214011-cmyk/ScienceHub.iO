# Rollback Foundation

Emergency rollback is governed by user-configured policy.

Where authorized by that policy, Upgrade Guard may perform a safety rollback, then immediately notify the user, record relevant history and allow review.
