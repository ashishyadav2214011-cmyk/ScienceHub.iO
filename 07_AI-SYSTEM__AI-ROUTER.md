# AI Router

Placement:
User → User Control → Upgrade Guard / AI Control → AI Router → Specialized AI

The Router selects/routes requests according to defined authority boundaries.

If a user selects an AI outside its authority, the Router must re-inform the user and cannot silently bypass hard boundaries.
